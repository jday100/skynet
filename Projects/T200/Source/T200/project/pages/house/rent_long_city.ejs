<form id="form" class="list_form" onsubmit="return false;">
    <input type="hidden" name="test">
    <input type="hidden" id="id" name="id" value="">
    <input type="hidden" name="page" value="1">
    <input type="hidden" name="search">
    <div class="paging">
        <% 
            if(paging){ 
                if(paging.first){ 
        %>
            <a href="#" onclick="turning(1, $.id('list_box'), '/house/rent/long/city/list');">First</a>
            <% 
                } 
                if(paging.pages){ 
                    paging.pages.forEach(item=> {
            %>
                <a href="#" onclick="turning(<%=item%>, $.id('list_box'), '/house/rent/long/city/list');"><%=item%></a>
            <% 
                }); 
            } 
                if(paging.last){ %>
                    <a href="#" onclick="turning(<%=paging.last%>, $.id('list_box'), '/house/rent/long/city/list');">Last</a>
        <% 
                } 
            } 
        %>
    </div>
    <div class="list_group">
        <% 
        if(0 < houses.length){ 
            let i = 0;
            houses.forEach(item=> {
        %>
            <div class="list_item">
                <div class="item_left">
                    <a href="/house/house_rent.html?id=<%=item.id%>"><%=item.title%></a>
                </div>
                <div>
                    <input type="hidden" id="time<%=i%>" value="<%=item.create_time%>">
                    <input disabled class="list_time" type="text" id="time_title<%=i%>" value="">
                </div>
            </div>
        <% 
            i++;
            }); 
        }else{ 
            %>
                <div class="list_item">
                    <div>
                        <a href="/house/house_rent.html">unpublished</a>
                    </div>
                    <div>
                        2023-10-10 10:10
                    </div>
                </div>
            <% 
        } 
        %>


    </div>
    <div class="paging">
        <% 
            if(paging){ 
                if(paging.first){ 
        %>
            <a href="#" onclick="turning(1, $.id('list_box'), '/house/rent/long/city/list');">First</a>
            <% 
                } 
                if(paging.pages){ 
                    paging.pages.forEach(item=> {
            %>
                <a href="#" onclick="turning(<%=item%>, $.id('list_box'), '/house/rent/long/city/list');"><%=item%></a>
            <% 
                }); 
            } 
                if(paging.last){ %>
                    <a href="#" onclick="turning(<%=paging.last%>, $.id('list_box'), '/house/rent/long/city/list');">Last</a>
        <% 
                } 
            } 
        %>
    </div>               
</form>