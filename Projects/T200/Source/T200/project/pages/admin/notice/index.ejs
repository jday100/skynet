
    <input type="hidden" name="test">
    <input type="hidden" name="page" value="1">
    <div class="search_box">
        <input type="text" name="search">
        <button id="search" class="search_button" onclick="hit_search('form', $.id('list_box'), '/notice/search');">Search</button>
    </div>
    <div class="paging_top_box">
        <div class="paging">
            <% 
                if(paging){ 
                    if(paging.first){ 
            %>
                <a href="#" onclick="turning(1, $.id('list_box'), '/notice/list');">First</a>
                <% 
                    } 
                    if(paging.pages){ 
                        paging.pages.forEach(item=> {
                %>
                    <a href="#" onclick="turning(<%=item%>, $.id('list_box'), '/notice/list');"><%=item%></a>
                <% 
                    }); 
                } 
                    if(paging.last){ %>
                        <a href="#" onclick="turning(<%=paging.last%>, $.id('list_box'), '/notice/list');">Last</a>
            <% 
                    } 
                } 
            %>
        </div>
    </div>
    <div class="list_group">
        <% 
        if(0 < notices.length){ 
            notices.forEach(item=> {
        %>
            <div class="list_item">
                <div>
                    <a onclick="hit('nid', <%=item.id%>);" href="/notice/notice.html"><%=item.title%></a>
                </div>
                <div>
                    2023-10-10 10:10
                </div>
            </div>
        <% 
            }); 
        }else{ 
            %>
                <div class="list_item">
                    <div>
                        <a href="/notice/notice.html">unpublished</a>
                    </div>
                    <div>
                        2023-10-10 10:10
                    </div>
                </div>
            <% 
        } 
        %>


    </div>
    <div class="paging_bottom_box">
        <div class="paging">
            <% 
                if(paging){ 
                    if(paging.first){ 
            %>
                <a href="#" onclick="turning(1, $.id('list_box'), '/notice/list');">First</a>
                <% 
                    } 
                    if(paging.pages){ 
                        paging.pages.forEach(item=> {
                %>
                    <a href="#" onclick="turning(<%=item%>, $.id('list_box'), '/notice/list');"><%=item%></a>
                <% 
                    }); 
                } 
                    if(paging.last){ %>
                        <a href="#" onclick="turning(<%=paging.last%>, $.id('list_box'), '/notice/list');">Last</a>
            <% 
                    } 
                } 
            %>
        </div>
    </div>
